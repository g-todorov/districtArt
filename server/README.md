# districtArt server-side

This folder contains the server implementation of the website.

## Build & development

Run `grunt` for serving the `express` server with `nodemon`.

## Linting

Running `grunt lint` will run the `eslint` linter.
